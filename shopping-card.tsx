// Implement a feature where the quantity of a single item is managed globally using Redux.
// • Requirements:
// 1. Set up a basic Redux store with a single slice/reducer to manage the quantity of an item (initial state: quantity: 0).
// 2. Create an Action for INCREMENT_QUANTITY and one for DECREMENT_QUANTITY.
// 3. Create a React component called QuantityControl.
// 4. This component should display the current quantity (retrieved using useSelector).
// 5. Include two buttons (+ and -) that dispatch the respective actions (using useDispatch).
// 6. Implement a constraint: the quantity cannot go below zero.

export const ShoppingCard = () => {

}